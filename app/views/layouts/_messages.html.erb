<% flash.each do |key, value| %>
    <div class="alert alert-<%= devise_key_to_bootstrap_class key %>">
      <a href="#" data-dismiss="alert" class="close">×</a>
      <ul>
        <li>
          <%= value %>
        </li>
      </ul>
    </div>
  <% end %>